export type Project = {
    id: string,
    type: string,
    attributes: {
    name: string,
    steps: string,
    description: string,
    features: string,
    interactions: string,
    colors: string,
    timeline: string,
    saved: string,
    user_id: string
    }
}
