version: 2.1 
orbs: 

  cypress: cypress-io/cypress@1
workflows: 
  build:
    jobs:
      - cypress/run: 
          start-command: 'npm run start'
          cypress-command: npx cypress run --headless 
      - build